# Retr01 UI library

Generic SDL widgets, draw helpers, and bundled Proggy Tiny. PNG chrome (radio, checkbox, dot) is injected by the host via `r01_ui_chrome_set`. Clickable tabs and a pager (prev / N/M / next, optional plane slot) both live here. Studio-specific widgets (palette grid, compose) stay in the Studio app.
